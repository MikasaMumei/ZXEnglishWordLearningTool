好的，这是一个清晰、简洁的**软件框架和文件索引树（File Index Tree）**，它概括了您整个项目的组成元件、功能和文件夹结构。

##⚙️ 软件框架组成元件您的工具由三个核心元件组成，它们在老师的电脑上协同工作：

| 元件 | 角色定位 | 核心技术 | 端口/协议 |
| --- | --- | --- | --- |
| **1. 前端应用 (Client)** | 学生/老师的用户界面。负责渲染、交互、本地进度存储、以及与后端通信。 | Vue3, Vite, Pinia, TS, Tailwind CSS | HTTP/8080 (静态文件), WebSocket/3678 |
| **2. 后端服务 (Server)** | 核心数据处理器。负责数据持久化、同步、词汇导入、排行榜计算和实时对战管理。 | Node.js (纯JS), SQLite3, ws (WebSocket), multer, xlsx | HTTP/3678 (API), WebSocket/3678 |
| **3. 数据层 (Data)** | 存储学生进度、词库和游戏化数据。 | SQLite (`.db` 文件) | N/A (本地存储) |

---

##📂 文件索引树 (File Index Tree)这个索引树展示了您最终项目部署时应有的完整结构。

###💻 根目录 (`english-studio-tool/`)```
english-studio-tool/
├── start.bat               # 【一键启动器】启动 serve.exe
├── serve.exe               # 【后端】打包后的 Node.js 服务器（监听 3678 端口）
├── package.json            # 后端依赖（sqlite3, ws, multer, xlsx）和打包脚本
├── data/                   # 【数据层】SQLite 数据库文件
│   ├── users.db            # 学生进度、金币、分数、错题本
│   └── words.db            # 结构化词库、音标、释义 (JSON 格式)
├── FileStruc.md            # 【文档】Excel 导入的结构说明 (供 AI 编程参考)
└── dist/                   # 【前端】Vite 打包后的纯静态文件
    ├── index.html
    ├── assets/
    │   └── ... (JS/CSS/字体)
    └── audio/              # 存放单词发音 MP3 文件

```

###🖥️ 前端源码目录 (`src/`) 结构```
src/
├── assets/                 # 样式、图片、图标
├── components/             # 可复用组件
│   ├── common/             # 按钮、进度条、徽章展示
│   ├── game/               # 对战/排行榜卡片
│   └── LearnModes/         # 四种背词模式组件（词->义, 义->词, 听音, 拼写）
├── stores/                 # Pinia 状态管理
│   ├── auth.ts             # 登录、用户认证、金币/等级状态
│   ├── learning.ts         # 单词数据、艾宾浩斯算法、错题本管理
│   ├── ranking.ts          # 排行榜数据获取
│   └── game.ts             # 【新增】WebSocket 连接、对战状态、分数同步
├── router/
│   └── index.ts            # 路由配置及 Admin 权限守卫
├── views/                  # 页面级组件
│   ├── LoginView.vue       # 登录/班级选择
│   ├── HomeView.vue        # 首页/任务进度/模式入口
│   ├── LearnView.vue       # 单词学习主界面
│   ├── RankView.vue        # 排行榜（周/月/总榜）
│   ├── GameView.vue        # 【新增】双人对战界面 (使用 WebSocket)
│   ├── WrongWordsView.vue  # 【新增】我的错题本练习界面
│   └── Admin/              # 老师管理后台
│       ├── AdminLogin.vue
│       └── AdminDashboard.vue  # 词库导入、任务布置、数据导出 (含错题导出 PDF)
├── App.vue
└── main.ts

```

---

##🎯 核心功能与位置概览| 功能模块 | 前端位置 (`src/`) | 后端 API / 协议 |
| --- | --- | --- |
| **词库导入/管理** | `views/Admin/AdminDashboard.vue` | `POST /admin/import-words` (HTTP) |
| **学生进度同步** | `stores/learning.ts` (Action) | `POST /sync` (HTTP) |
| **艾宾浩斯复习** | `stores/learning.ts` (Getter) | 依赖本地计算和 `users.db` 数据 |
| **错题本练习** | `views/WrongWordsView.vue` | 依赖本地和 `users.db` 数据 |
| **错题本导出打印** | `views/Admin/AdminDashboard.vue` | `POST /admin/export-wrong-words` (HTTP) |
| **双人实时对战** | `stores/game.ts` / `views/GameView.vue` | **WebSocket** (`ws://IP:3678/ws`) |
| **排行榜显示** | `views/RankView.vue` | `GET /data` (HTTP) |
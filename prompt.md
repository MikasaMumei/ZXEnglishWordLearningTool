
##🎯 完整 AI 编程提示词清单 (Docker & 里程碑模式)###阶段 1：核心后端与数据初始化 (Prompt 1)**目标：** 建立 Node.js 后端环境、SQLite 数据库结构、基础 API 和 Dockerfile。

> **角色设定：** 你是一名精通 Node.js、SQLite、WebSocket 和 Docker 的全栈工程师。
> **目标：** 创建一个极简的 Node.js 后端服务 (`server.js`)，用于支持离线局域网学习系统。同时，你需要为该服务编写 Dockerfile。
> **要求：**
> 1. **服务环境：** 使用纯 Node.js，监听 **3678 端口**。依赖 `sqlite3`, `multer`, `xlsx`, `ws` 库。
> 2. **启动与初始化：** 启动时必须检查并创建 `data` 文件夹、`users.db` 和 `words.db`。Node.js 服务应在数据库初始化完成后，才开始监听连接。
> 3. **数据结构：**
> * `users.db` 核心表 `users` (`id`, `name`, `class`, `progress` [JSON], `coins`, `score`)。
> * `words.db` 核心表 `words` (`id` INTEGER PRIMARY KEY, `word` TEXT, `meaning` **JSON**, `phonetic_uk` TEXT, `phonetic_us` TEXT, `type` TEXT, `level` INTEGER)。
> 
> 
> 4. **核心 API 接口 (HTTP)：**
> * **POST /sync：** 接收学生端 JSON 数据，合并或替换到 `users.db`。
> * **GET /data：** 返回最新的全局排行榜和所有词库数据。
> * **GET /admin：** 返回一个极简的 HTML 管理登录页面（密码：`123456`）。
> * **POST /admin/export-wrong-words：** （仅建立接口框架，无需实现复杂的 PDF 导出逻辑）接收学生ID和格式参数。
> 
> 
> 5. **Excel 导入接口：** **POST /admin/import-words**。
> * **导入规则：** **请翻看 FileStruc.md 文件，参考其中的 Excel 结构**。
> * **释义解析：** 导入时，必须将释义字段中通过**换行符分隔**的词性-释义对，解析为结构化的 **JSON 数组**。
> 
> 
> 6. **WebSocket 接口：** 必须集成 `ws` 库，在 `/ws` 路径监听连接，为双人对战做准备。
> 7. **Docker 文件：** 编写 **`Dockerfile.backend`**，用于构建 Node.js 服务容器。
> 
> 
> **输出要求：**
> * 完整的 `server.js` 文件。
> * 完整的 `Dockerfile.backend` 文件。
> * `FileStruc.md` 文件内容。
> * 依赖安装（`npm install ...`）说明。
> 
> 

---

###阶段 2：前端项目骨架与配置 (Prompt 2)**目标：** 初始化前端项目结构、配置 Vite/Tailwind，并为 Docker 环境准备 API 访问路径。

> **角色设定：** 你是一名精通 Vue3、Vite 和 Tailwind CSS 的前端架构师。
> **目标：** 初始化一个完整的 Vue3 (Composition API + `<script setup>`) + Vite + TypeScript + Tailwind CSS 4.0 项目骨架。
> **要求：**
> 1. **项目配置：**
> * 创建 `package.json`，包含所有必要的依赖（含 `websocket` 客户端库）。
> * 配置 `vite.config.ts`，设置 `base: './'`。
> * 配置 `tailwind.config.js`，包含适合 6～15 岁学生的**精美卡通风格配色**。
> 
> 
> 2. **API 配置：** 所有后端 API 请求地址必须配置为相对于 Docker 网络的 URL：**`http://backend:3678`**。
> 3. **结构搭建：**
> * 创建 `src/router/index.ts`（包含登录、首页路由）。
> * 创建核心文件夹：`src/stores` (含 `auth.ts`, `learning.ts`, `ranking.ts`, **`game.ts`**), `src/views`, `src/components`。
> * **音频说明：** 在 `public/audio` 文件夹说明中，明确音频文件应命名为 `[word_id].mp3`。
> 
> 
> 
> 
> **输出要求：** 完整输出 `package.json`、`vite.config.ts`、`tailwind.config.js` 和 `src/router/index.ts` 的基础代码。

---

###阶段 3：核心状态管理与数据同步 (Prompt 3)**目标：** 实现前端所有 Pinia Stores 的核心逻辑，包括艾宾浩斯算法数据结构、错题追踪和数据同步。

> **角色设定：** 你是一名精通 Pinia 和 TS 的前端架构师。
> **目标：** 实现前端系统的核心 Pinia 状态管理。
> **要求：**
> 1. **用户认证 Store (`stores/auth.ts`)：** 实现登录 Action 和用户基本状态 (ID, Name, Class, Coins)。
> 2. **学习数据 Store (`stores/learning.ts`)：**
> * **进度存储结构：** 明确 `progress` JSON 结构为：`{'wordId': { status: 'new'/'learning'/'mastered', lastReview: 'YYYY-MM-DD', nextReview: 'YYYY-MM-DD', reviewInterval: 1, wrongCount: 0 }}`。
> * **艾宾浩斯 Getter：** 实现 `getTodayWords()` Getter。
> * **错题本 Action：** 增加 `addWrongWord(wordId, type)` Action。
> * **干扰项生成：** 增加 `getQuizOptions(correctWordId)` Getter，用于从词库中生成包含 3 个相关干扰项的选项列表。
> 
> 
> 3. **对战 Store (`stores/game.ts` - 新增)：** 定义对战状态 State 和 `connectWebSocket()`, `initiateMatch()`, `sendAnswer()` Action 的结构。
> 4. **同步 Action：** 实现核心的 `syncDataToBackend()` Action，将变动数据 POST 到 `http://backend:3678/sync`。
> 
> 
> **输出要求：** 完整输出 `src/stores/auth.ts`、`src/stores/learning.ts` 和 `src/stores/game.ts` 的核心代码结构。

---

###阶段 4：基础学习界面与核心 UI (Prompt 4)**目标：** 完成学生端的核心页面布局、导航和基础学习模式。

> **角色设定：** 你是一名精通 Vue3 和 Tailwind CSS 的前端工程师。
> **目标：** 实现学生端的核心 UI 视图和基础学习模式。
> **要求：**
> 1. **首页 (`views/HomeView.vue`)：**
> * 保持圆环进度、金币/等级显示。
> * 模式入口：包含“词→义”、“义→词”、“听音选义”、“拼写输入”、“**我的错题本**” **五个**模式。
> * **对战入口：** 增加一个醒目的“**双人单词挑战**”按钮。
> 
> 
> 2. **背词模式 (`WordToMeaning.vue`)：** 实现基础的“**词→义**”模式组件，能正确显示单词、音标和**结构化释义**中的一个随机选项，并能调用 `addWrongWord()`。
> 3. **对战/错题路由：** 完成 `views/GameView.vue` 和 `views/WrongWordsView.vue` 的空壳视图。
> 
> 
> **输出要求：** 完整输出 `src/views/HomeView.vue` 和 `src/components/LearnModes/WordToMeaning.vue` 的完整代码。

---

###阶段 5：高级功能实现（对战、错题本、管理后台）(Prompt 5)**目标：** 实现实时对战、错题本视图、以及老师管理后台的数据导出。

> **角色设定：** 你是一名全栈工程师，专注于复杂交互和数据处理。
> **目标：** 实现对战、错题本和管理后台的最终功能。
> **要求：**
> 1. **对战视图 (`views/GameView.vue`)：** 实现 WebSocket (`game.ts`) 连接、匹配/游戏逻辑的 UI 交互，显示分数和题目。
> 2. **错题本视图 (`views/WrongWordsView.vue`)：** 实现错题列表显示和针对错题的专项练习功能。
> 3. **管理后台 (`views/Admin/AdminDashboard.vue`)：**
> * **路由保护：** 确保 `/admin` 路由受密码保护。
> * **错题本导出功能：** 界面上提供导出按钮，调用后端 **POST /admin/export-wrong-words** 接口。
> * **导出格式选择：** 界面必须允许选择**格式 1 (完整：含音标)** 和 **格式 2 (挖空)**。
> 
> 
> 4. **后端导出实现：** 补充 `server.js` 中 **POST /admin/export-wrong-words** 接口的逻辑，实现根据错题数据生成符合 **A4 双列排版**要求的 HTML 或 PDF 文档。
> 
> 
> **输出要求：** 完整输出 `views/GameView.vue`、`views/WrongWordsView.vue` 和 `views/Admin/AdminDashboard.vue` 的代码。

---

###阶段 6：Docker Compose 部署与最终脚本 (Prompt 6)**目标：** 创建完整的 Docker 容器化部署方案，并提供一键启动脚本。

> **角色设定：** 你是一名 DevOps 工程师。
> **目标：** 为整个系统创建完整的 Docker 容器化部署方案。
> **要求：**
> 1. **前端容器 (`Dockerfile.frontend`)：**
> * 使用多阶段构建：第一阶段用 Node/Vite 构建 Vue3 项目 (`npm run build`)。
> * 第二阶段使用 **Nginx 基础镜像**，托管 `dist` 文件夹中的静态文件，并在 **80 端口**提供服务。
> 
> 
> 2. **服务编排 (`docker-compose.yml`)：**
> * 定义 `backend` (使用 `Dockerfile.backend`) 和 `frontend` 两个服务。
> * **网络设置：** 确保 `frontend` 可以通过服务名 `backend:3678` 访问 Node.js 服务。
> * **持久化：** 为 `backend` 服务的 `data` 目录配置一个 **命名卷 (Named Volume)**，以确保存储的 `.db` 文件是持久化的。
> * **端口映射：** 将 `frontend` 容器的 80 端口映射到宿主机的 **8080 端口**。
> * **健康检查：** 配置 `backend` 服务的 **`HEALTHCHECK`**，确保 Node.js 应用完全就绪。
> 
> 
> 3. **启动脚本：** 创建一个 `start-docker.bat` 脚本，用于执行 `docker compose up -d`。
> 
> 
> **输出要求：** 完整输出 `Dockerfile.frontend`、`docker-compose.yml` 和 `start-docker.bat` 的代码内容。